/**
 * Test store addons
 */

describe('store', () => {
  it('store', () => {
    // const { container } = render(<TransactionDataPage />);
    // expect(container).toMatchSnapshot();
    expect('a').toMatch('a');
    expect(1).toBe(1);
  });
});
