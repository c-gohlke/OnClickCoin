import getPermission from '../getPermission';

describe('getPermission()', () => {
  it('functions properly', () => {
    // expect(getPermission(something).toEqual(somethingElse));
    expect(1).toBe(1);
  });
});
