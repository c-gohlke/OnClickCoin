import deployContract from '../deployContract';

describe('deployContract()', () => {
  it('functions properly', () => {
    // expect(deployContract(something).toEqual(somethingElse));
    expect(1).toBe(1);
  });
});
