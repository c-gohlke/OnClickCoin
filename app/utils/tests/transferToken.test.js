import transferToken from '../transferToken';

describe('transferToken()', () => {
  it('functions properly', () => {
    // expect(transferToken(something).toEqual(somethingElse));
    expect(1).toBe(1);
  });
});
