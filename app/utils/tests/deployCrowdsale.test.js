import deployCrowdsale from '../deployCrowdsale';

describe('deployCrowdsale()', () => {
  it('functions properly', () => {
    // expect(deployCrowdsale(something).toEqual(somethingElse));
    expect(1).toBe(1);
  });
});
