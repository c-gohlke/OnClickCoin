import netIDtoName from '../netIDtoName';

describe('netIDtoName()', () => {
  it('functions properly', () => {
    // expect(netIDtoName(something).toEqual(somethingElse));
    expect(1).toBe(1);
  });
});
