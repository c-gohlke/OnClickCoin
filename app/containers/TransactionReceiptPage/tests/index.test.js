// import React from 'react';
// import { render } from '@testing-library/react';
// import TransactionReceiptPage from '../index';

describe('<TransactionReceiptPage />', () => {
  it('renders and matches the snapshot', () => {
    // const { container } = render(<TransactionReceiptPage />);
    // expect(container).toMatchSnapshot();
    expect('a').toMatch('a');
    expect(1).toBe(1);
  });
});
