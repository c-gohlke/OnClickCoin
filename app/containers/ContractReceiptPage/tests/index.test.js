// import React from 'react';
// import { render } from '@testing-library/react';
// import ContractReceiptPage from '../index';

describe('<ContractReceiptPage />', () => {
  it('renders and matches the snapshot', () => {
    // const { container } = render(<ContractReceiptPage />);
    // expect(container).toMatchSnapshot();
    expect('a').toMatch('a');
    expect(1).toBe(1);
  });
});
